spring:
  application:
    name: instogram
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view=false:
server:
  port: 8080

security:
  secret:
    key: java
  springdoc:
    swagger-ui:
      path: /api-docs/swagger-ui.html